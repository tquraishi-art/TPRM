# TPRM Platform — environment variables
# Copy this file to .env.local and fill in values for local development
# Never commit .env.local to version control

# Base URL for the backend API (Node/Express server)
# Defaults to /api in production (proxied by nginx or same-origin)
VITE_API_URL=http://localhost:8000
